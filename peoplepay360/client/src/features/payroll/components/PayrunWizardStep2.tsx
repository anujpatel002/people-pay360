export default function PayrunWizardStep2({ids,setIds,onBack,onCreate}:{ids:string[];setIds:(v:string[])=>void;onBack:()=>void;onCreate:()=>void}){return <section><h3>2. Select employees</h3><textarea placeholder="Employee IDs, one per line" onChange={e=>setIds(e.target.value.split(/\s+/).filter(Boolean))}/><p>{ids.length} selected</p><button onClick={onBack}>Back</button><button disabled={!ids.length} onClick={onCreate}>Create Draft Payrun</button></section>}
